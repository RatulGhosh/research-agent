Healthcare agents often fail long workflows even when they can complete many individual steps. Existing benchmarks measure task success, but they do not test whether successful agent traces can be converted into reusable workflow skills that help on new cases. We study whether skills distilled from verifier-approved traces improve performance on held-out healthcare tasks while preventing unsafe transfer, such as sending a fax or closing a case when the workflow requires the agent to stop.
